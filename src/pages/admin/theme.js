// theme.js

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#10898d', // main colror of iconsssssssssssssss
        },
        inherit: {
            main: '#284b63', // alter  color wwhn the curser shfts  
        },
        type: 'dark', // Use a light theme
    },
});

export default theme;
